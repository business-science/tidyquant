# tidyquant palettes for use with scales

These palettes are mainly called internally by tidyquant `scale_*_tq()`
functions.

## Usage

``` r
palette_light()

palette_dark()

palette_green()
```

## Examples

``` r
library(scales)
scales::show_col(palette_light())

```
